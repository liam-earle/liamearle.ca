---
import { TypeAnimation } from 'react-type-animation';
const { subject, subjectLink, cursor } = Astro.props
---
<div class="block p-6 max-w-4xl w-full bg-gray-900 rounded-lg border border-gray-800 shadow-md">
	<a class="mb-2 text-4xl font-code-font font-bold tracking-tight text-gray-200" href={subjectLink}>
		<TypeAnimation
			client:load
			sequence={[
				subject,
			]}
			wrapper="div"
			cursor={cursor}
		/>
	</a>
	<div class="font-code-font text-2xl text-gray-200 drop-shadow-lg">
		<slot />
	</div>
</div>